import { TimePickerThemeDirective } from './time-picker-theme.directive';

describe('TimePickerThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new TimePickerThemeDirective();
    expect(directive).toBeTruthy();
  });
});
